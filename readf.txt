app3: 
added loader if no data found, 
showing loader if response takes time: loading.

app2: function code  udpated to variables and added promises --sync  await


only app.js is changed in all httpReq 1 2 3 files